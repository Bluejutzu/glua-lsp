# Changelog

Notable changes to `glua-core` — the parser, analyser, formatter and LSP
engine shared by [`glua-gmod`](../glua-lsp/CHANGELOG.md) (the VS Code
extension) and [`glua-cli`](../glua-cli/CHANGELOG.md). This package is
never published; the version here only tracks what changed in the engine
itself, ahead of either consumer deciding to pick it up.

## Unreleased

_Nothing yet._
